import { FilterPersonnesPipe } from './filter-personnes.pipe';

describe('FilterPersonnesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPersonnesPipe();
    expect(pipe).toBeTruthy();
  });
});
